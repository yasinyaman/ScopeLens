# Release automation: push a tag like `v0.1.0a1` →
#   1. re-run the full check suite (same as CI),
#   2. build + push the Docker image to GHCR (ghcr.io/<owner>/etki),
#   3. build the sdist/wheel and publish to PyPI (trusted publishing),
#   4. create a GitHub Release with the matching CHANGELOG section.
#
# One-time setup required for PyPI: a trusted publisher on pypi.org PER PACKAGE
# (all workflow: release.yml) — project `etki` env `pypi`, project `etki-api` env
# `pypi-etki-api`, project `etki-plugin-jira` env `pypi-etki-plugin-jira`. PyPI
# refuses two pending publishers with the identical repo+workflow+environment
# config, so the environment name is the disambiguator (one job each). Until a
# package's publisher is configured its publish job fails; the others are
# independent. (etki-plugin-linear stays workspace-only — no PyPI job yet.)
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write # GitHub Release
  packages: write # GHCR push
  id-token: write # PyPI trusted publishing

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
          enable-cache: true
      - run: uv sync --frozen --dev
      - run: uv run ruff check .
      - run: uv run mypy etki packages/etki-api/src/etki_api packages/etki-plugin-linear/src/etki_plugin_linear packages/etki-plugin-jira/src/etki_plugin_jira
      - run: uv run pytest --cov=etki --cov=etki_api --cov-fail-under=70
      - run: uv run python -m eval.runner

  docker:
    needs: checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/etki
          tags: |
            type=pep440,pattern={{version}}
            type=raw,value=latest
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # PyPI rejects two pending trusted publishers with the IDENTICAL config
  # (repo + workflow + environment) — the environment name is the disambiguator.
  # So each package publishes from its OWN job/environment:
  #   project `etki`     ← environment `pypi`
  #   project `etki-api` ← environment `pypi-etki-api`
  # skip-existing lets a tag re-run without failing on versions already uploaded
  # (etki-api only uploads when its version was actually bumped).
  pypi:
    needs: checks
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv build # root member only = etki
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true

  pypi-etki-api:
    needs: checks
    runs-on: ubuntu-latest
    environment: pypi-etki-api
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv build --package etki-api
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true

  # The first WRITING plugin — published so a verified-marketplace install can
  # resolve its `etki-api>=0.1.2,<0.2` dependency from PyPI. Its wheel carries no
  # workspace source (uv.sources is a dev-only override), so it resolves etki-api
  # from PyPI like any third-party consumer.
  pypi-etki-plugin-jira:
    needs: checks
    runs-on: ubuntu-latest
    environment: pypi-etki-plugin-jira
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv build --package etki-plugin-jira
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true

  github-release:
    # NOT dependent on PyPI: the image + GitHub Release still go out even if the
    # trusted publisher isn't configured yet (the pypi job can fail independently).
    needs: [docker]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract changelog section for this version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          awk -v ver="$VERSION" '
            $0 ~ "^## \\[" ver "\\]" {found=1; next}
            found && /^## \[/ {exit}
            found {print}
          ' CHANGELOG.md > release-notes.md
          if ! [ -s release-notes.md ]; then
            echo "No CHANGELOG section for $VERSION; using a generic note." >&2
            echo "See CHANGELOG.md for details." > release-notes.md
          fi
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "Etki $GITHUB_REF_NAME" \
            --notes-file release-notes.md \
            $([[ "$GITHUB_REF_NAME" == *a* || "$GITHUB_REF_NAME" == *b* || "$GITHUB_REF_NAME" == *rc* ]] && echo --prerelease)
